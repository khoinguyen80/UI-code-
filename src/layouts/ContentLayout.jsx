const ContentLayout = ({ children }) => (
  <div
    style={{
      overflow: 'auto',
      padding: '70px 10px 50px 10px',
      background: '#ffffff',
      height: '100%',
    }}
  >
    {children}
  </div>
)

export default ContentLayout
